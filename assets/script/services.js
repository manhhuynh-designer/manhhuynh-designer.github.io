// services.js - Simplified for Swiss Design
// Carousel logic removed in favor of clean grid layout
document.addEventListener("DOMContentLoaded", () => {
  // Add hover effects or entry animations for services if needed
  const serviceCards = document.querySelectorAll('.glass-card');
  
  serviceCards.forEach(card => {
    card.addEventListener('mouseenter', () => {
      // Logic for hover can be added here or handled via CSS
    });
  });
});
